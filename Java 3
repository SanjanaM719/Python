There are three lines of input:

The first line contains an integer.
The second line contains a double.
The third line contains a String.
Output Format

There are three lines of output:

On the first line, print String: followed by the unaltered String read from stdin.
On the second line, print Double: followed by the unaltered double read from stdin.
On the third line, print Int: followed by the unaltered integer read from stdin.



import java.util.Scanner;

public class InputOutputExample {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Reading an integer from the first line
        int integerInput = scanner.nextInt();
        // Consuming the newline character
        scanner.nextLine();

        // Reading a double from the second line
        double doubleInput = scanner.nextDouble();
        // Consuming the newline character
        scanner.nextLine();

        // Reading a String from the third line
        String stringInput = scanner.nextLine();

        // Closing the scanner
        scanner.close();

        // Printing the output
        System.out.println("String: " + stringInput);
        System.out.println("Double: " + doubleInput);
        System.out.println("Int: " + integerInput);
    }
}
